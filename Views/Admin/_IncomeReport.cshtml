@model IEnumerable<dynamic>

<h3>💰 Báo cáo thu nhập trong tháng</h3>

@if (!Model.Any())
{
    <p>Chưa có dữ liệu hóa đơn trong tháng này.</p>
}
else
{
    <table class="data-table">
        <thead>
            <tr>
                <th>Ngày</th>
                <th>Số đơn</th>
                <th>Doanh thu (VNĐ)</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var day in Model)
            {
                <tr>
                    <td>@day.Date.ToString("dd/MM/yyyy")</td>
                    <td>@day.OrderCount</td>
                    <td>@String.Format("{0:N0}", day.Total)</td>
                </tr>
            }
        </tbody>
    </table>
    <p style="margin-top:10px;">
        <strong>Tổng doanh thu tháng này:</strong> 
        @String.Format("{0:N0} VNĐ", ViewBag.TotalMonth)
    </p>
}
